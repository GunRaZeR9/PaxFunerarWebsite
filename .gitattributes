# Shell scripts must stay LF — they run on the Linux WordPress host and CRLF
# would break the shebang / bash parsing.
*.sh text eol=lf

# Treat theme assets as binary (no EOL munging).
*.png binary
*.jpg binary
*.svg text
